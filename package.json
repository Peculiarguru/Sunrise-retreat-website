{
  "name": "Villa design",
  "version": "1.0.0",
  "description": "This is design of our website",
  "scripts": {
    "start": "rm -rf dev-serve && parcel src/booking.html --dist-dir dev-serve",
    "build": "rm -rf build && parcel build src/index.html --dist-dir build --public-url ./",
    "publish": "gh-pages -d build"
  },
  "author": "Pecu Irina Fazan",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
